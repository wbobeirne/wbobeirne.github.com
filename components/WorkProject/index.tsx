import React from "react";

interface WorkProjectProps {
  logo: React.ReactNode;
  id: string;
  title: string;
  description: React.ReactNode;
}

export const WorkProject: React.FC<WorkProjectProps> = ({ logo }) => {
  return <div></div>;
};
